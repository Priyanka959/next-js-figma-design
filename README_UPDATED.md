# design-figma-2

A Next.js (App Router) UI kit / design implementation based on Figma — a simple marketing/site frontend prototype.

## Features
- Next.js 13+ with App Router
- TypeScript-ready layout
- Tailwind CSS integration
- Components for home, contact, and shared UI
- Photo gallery and lightbox dependencies included

## Prerequisites
- Node.js 18+ and npm (or yarn)

## Install
```bash
npm install
```

## Development
Start the dev server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
npm run start
```

Lint:

```bash
npm run lint
```

## Project structure (selected)
- `src/app/` — Next.js app routes and pages
- `src/components/` — Reusable UI components (home, contact, shared)
- `public/assests/` — Static assets
- `package.json` — Scripts and dependencies

## Notes
- Uses `next`, `react`, `react-dom` and Tailwind CSS. See `package.json` for exact versions and scripts.

## Contributing
Feel free to open issues or submit PRs for fixes and small improvements.

## License
Specify a license if desired.
